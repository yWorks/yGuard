name: Continous Examples for yGuard

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Set up JDK 1.8
        uses: actions/setup-java@v1
        with:
          java-version: 1.8
      - name: Local publication for consumption by examples
        run: ./gradlew publishToMavenLocal
      - name: Test annotation example
        working-directory: example/annotation
        run: ./gradlew obfuscate
      - name: Test application example
        working-directory: example/application
        run: ./gradlew obfuscate
      - name: Test external_library example
        working-directory: example/external_library
        run: ./gradlew obfuscate
      - name: Test library example
        working-directory: example/library
        run: ./gradlew obfuscate
      - name: Test resources example
        working-directory: example/resources
        run: ./gradlew obfuscate
      - name: Test serializable_exclusion example
        working-directory: example/serializable_exclusion
        run: ./gradlew obfuscate
